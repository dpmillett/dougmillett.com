{
  "name": "dougmillett.com",
  "description": "Website for Doug Millett",
  "version": "1.0.0",
  "author": "drewbolles@gmail.com",
  "dependencies": {
    "gatsby": "^1.9.279",
    "gatsby-link": "^1.6.46",
    "gatsby-plugin-google-fonts": "^0.0.4",
    "gatsby-plugin-react-helmet": "^2.0.11",
    "gh-pages": "^1.2.0",
    "react-helmet": "^5.2.0"
  },
  "keywords": [
    "gatsby"
  ],
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "safari > 7"
  ],
  "license": "MIT",
  "scripts": {
    "build": "gatsby build",
    "develop": "gatsby develop",
    "format": "prettier --write 'src/**/*.js'",
    "deploy": "gatsby build --prefix-paths && gh-pages -d public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "prettier": "^1.14.3"
  }
}
